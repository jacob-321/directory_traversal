<h1>File Viewer</h1>
<p>Use the URL parameter <code>?file=filename</code> to view a file.</p>
<p>Example: <code>/files/show?file=safe.txt</code></p>
<p>You might try something like <code>../secrets/config.yml</code> if you're curious...</p>

<%= link_to 'Back', root_path%>